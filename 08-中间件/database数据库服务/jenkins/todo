1. gitlab+ jenkins --标准化  done

2. jenkins 用户组  done

3. jenkins agents  -agent 部署？统一名字/依赖 done
    需求：
        哪些job才可以跑在某些agent上

4. jenkins pipeline /jenkins file doing
    需求：
        １．pipeline如何接入
        ２．pipeline语法

5. jenkins 插件 总结 doing